package tech.codeabsolute.parkingapp.android

import org.junit.jupiter.api.Assertions.assertEquals
import org.junit.jupiter.api.Test

class ExampleTest {

    @Test
    fun `two plus two is four`() {

        val expected = 4

        val sum = 2 + 2

        assertEquals(expected, sum)
    }
}